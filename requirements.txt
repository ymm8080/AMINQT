# ── Data sources ──────────────────────────────────────────────
akshare>=1.12.0
# iFinDPy  : NOT on pip — install via 同花顺 iFinD terminal
# xtquant  : NOT on pip — install via miniQMT client

# ── Data / compute ────────────────────────────────────────────
pandas>=2.0
numpy>=1.24
scipy

# ── ML ────────────────────────────────────────────────────────
torch>=2.0
scikit-learn>=1.3
lightgbm>=4.0

# ── Web / scheduling ──────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.27
apscheduler>=3.10

# ── Visualization ─────────────────────────────────────────────
streamlit>=1.32
plotly>=5.20

# ── Utils ─────────────────────────────────────────────────────
pyyaml
python-dotenv

# ── Test ──────────────────────────────────────────────────────
pytest>=7.4
httpx>=0.25
