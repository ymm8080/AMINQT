## 概述

实施计划 v3.1 **P20.4 V5.1 检查清单**专项核验包: 纯函数 AST 审计 + 熊市接管 + 买卖规则口径 + S8 跌停逃生协议。

> **基底说明**: P20 栈位于 `feat/bucket-ic` (PR #24 合入此处, PR #26 bucket-ic→main 进行中), 故本 PR 以 `feat/bucket-ic` 为基底。待 #26 合入后 diff 自动收敛。

## 覆盖检查清单条目

| 条目 | 内容 | 落地 |
|:---|:---|:---|
| #2 | trigger() 纯函数审计 | `TestTriggerPurityAudit` — **AST 级**静态审计 (import 白名单 + 禁止调用清单 + 确定性行为复核), 取代弱正则 |
| #11 | 熊市协议接管 | `TestBearTakeover` — DEFENSE 全时段禁买 / RECOVERY 早盘关+尾盘开; **补规格缺口**: `sessions.position_cap()` (RECOVERY 30% 仓位上限, V5.1 §2 原已写明但代码未落地) |
| #5 | B5 双输入取小 + 样本外衰减 | `test_b5_uses_min_of_dual_inputs` / `test_b5_oos_decay_discount` |
| #5a | B7 止损距离否决 | `test_b7_no_stop_price_means_no_protection` (无止损价=无保护→否决) |
| #7 | S1 stop_price 下发制 | `test_s1_no_global_fixed_stop` (stop_price=0 跌破-4%也不触发, 证明无全局固定止损) |
| #8 | S2 ATR 自适应回撤带 | `test_s2_requires_3pct_profit_to_activate` (浮盈<3%不激活) |
| #10 | S8 跌停逃生协议 | `TestS8EscapeProtocol` — 优先级碾压 S3/S6/S1 / 仅跌停价触发 / T+1 锁死优先 / 回测排队不减仓不动现金 |

## 改动

- `app/intraday/v51/sessions.py` (+15): `RECOVERY_POSITION_CAP = 0.30` + `position_cap()` 纯函数 (单点引用, 与 buy_window_open 同口径)
- `tests/test_intraday_v51_p204.py` (新建, 16 tests)

## 验证

- `pytest tests/test_intraday_v51_p204.py` — 16/16 通过
- v51 全部 — 59/59 通过
- 全量 `pytest tests/` — **701 passed, 8 skipped**, 无回归
- `ruff check app/ scripts/ services/ tests/` — All checks passed

🤖 Generated with [Claude Code](https://claude.com/claude-code)
