# -*- coding: utf-8 -*-
"""Tests for app/models/lstm_model — forward shape (Phase 3)."""
import pytest

pytest.skip("lstm_model tests — implement in Phase 3 (needs torch)",
            allow_module_level=True)

# TODO: test forward(batch, 20, 30) → shape (batch,).
